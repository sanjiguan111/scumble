source 'https://rubygems.org'

# Ruby version is managed by mise (repo-root mise.toml). Deliberately no
# `ruby` directive: rvm's cd hook cannot parse range constraints and errors
# on them ("Unknown ruby interpreter version: >=3.0.0").

# Pin CocoaPods so every contributor resolves Podfile.lock with the same
# version (React Native convention). Install deps with `bundle install`, then
# run `bundle exec pod install` (packages/example/scripts/run-ios.mjs does this
# automatically).
gem 'cocoapods', '~> 1.16'
# Track the Lynx SDK's 4.0.x line — the 4.1 plugin line ships alongside the
# harmony-only SDK 4.1.0, which iOS never got.
gem 'cocoapods-lynx-library', '~> 4.0.2'
